// 7.1 
第 8 行定义了一个 i, 第 9 行后 i 的值为 0. 

随后进入循环. 循环头内的 i 是外层 i, 循环体内定义了一个具有 static storage duration 的名为 i 的变量, shadow 了外层变量 i. 

第一次循环结束内层 i 变为 2, 第二次循环结束内层 i 变为 6, 第三次循环结束内层 i 变为 42

最后的 printf 引用的 i 为外层 i, 此时其值 2.

故最后的输出为 2,6,42,2